name: data
publish_to: none
version: 0.0.1
homepage:

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  build_runner: 2.4.9
  json_serializable: 6.8.0

  retrofit: ^4.1.0
  dio: ^5.5.0+1

  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
